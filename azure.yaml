name: service-invoke-dapr-node-aca
metadata:
  template: service-invoke-dapr-node-aca@0.0.1-beta
services:
  checkout:
    project: checkout
    language: js
    host: containerapp
    module: app/checkout-service
  order-processor:
    project: order-processor
    language: js
    host: containerapp
    module: app/order-processor-service
 